apiVersion: v1
kind: Service
metadata:
  name: cruder
  namespace: default
  labels:
    app: cruder
  annotations:
    description: "Service for Cruder application"
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: http
    protocol: TCP
  selector:
    app: cruder
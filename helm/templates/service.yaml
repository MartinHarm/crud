apiVersion: v1
kind: Service
metadata:
  name: {{ include "cruder.fullname" . }}
  labels:
    {{- include "cruder.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cruder.selectorLabels" . | nindent 4 }}